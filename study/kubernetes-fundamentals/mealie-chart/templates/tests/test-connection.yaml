apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mealie-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mealie-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mealie-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
